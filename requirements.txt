# core retrieval stack
sentence-transformers>=2.7.0
rank-bm25>=0.2.2
numpy>=1.26
pyyaml>=6.0

# storage
pymongo>=4.6
motor>=3.5
qdrant-client>=1.9
neo4j>=5.20

# api
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-dotenv>=1.0
httpx>=0.27

# notebooks + plotting
jupyter>=1.0
matplotlib>=3.8
pandas>=2.2

# pdf + ingestion
pymupdf>=1.24
arxiv>=2.1

# tests
pytest>=8.0
pytest-anyio
anyio

# D3 — GraphRAG, evaluation, safety
transformers>=4.40        # already a sentence-transformers dependency; pinned explicitly since
                           # app/graphrag.py's Generator interface (D3 brief) will load Qwen2.5 directly
accelerate>=0.30          # multi-device model loading for the D3 generator
bitsandbytes>=0.43         # 4-bit quantization for the D3 generator — installs and imports fine on
                           # Windows/CPU, but 4-bit loading itself needs CUDA, which this dev machine
                           # doesn't have; see app/graphrag.py module docstring
# ragas is intentionally NOT pinned yet — the brief lists it as an optional
# cross-check against the local NLI/embedding proxy (see app/safety.py,
# scripts/evaluate_d3.py docstrings), deferred until the real generator is
# wired up since a 1.5B-judge RAGAS run isn't meaningful before then
